// libs
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  newsScreenWrapper: {
    flex: 1,
    marginTop: -40,
    marginBottom: -30,
    padding: 10
  }
});

export default styles;
