// libs
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  newsDetailScreenWrapper: {
    flex: 1
  },
  loadingView: {
    position: "absolute",
    top: 0,
    left: 0,
    bottom: 0,
    right: 0
  }
});

export default styles;
